package com.sai.datastructures.lists;

public class One_IntroductionLists {

    //The Java.util.List is a child interface of Collection. It is an ordered collection of objects in which duplicate
    // values can be stored. Since List preserves the insertion order, it allows positional access and insertion of
    // elements.

    ///List Interface is implemented by the classes of ArrayList, LinkedList, Vector and Stack.

    //Declaration:
    // List<Type> store = new ArrayList<>();
    // List<Type> store = new LinkedList<>();
    // List<Type> store = new Vector<>();
    // List<Type> store = new Stack<>();

    //Supported operations:
    //void add(int index, Object O) : This method adds given element at specified index.
    //boolean addAll(int index, Collection c) :
}
