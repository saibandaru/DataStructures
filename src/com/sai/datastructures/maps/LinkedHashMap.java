package com.sai.datastructures.maps;

public class LinkedHashMap {

    /*
     * HashMap + remember the order of insertion = LinkedHashMap
     *
     * Declaration:
     * LinkedHashMap<key-type, value-type> lhm = new LinkedHashMap<>();
     *
     * other declarations:
     * LinkedHashMap<key-type, value-type> lhm = new LinkedHashMap<>(int capacity);
     * LinkedHashMap<key-type, value-type> lhm = new LinkedHashMap<>(Map map);
     * LinkedHashMap<key-type, value-type> lhm = new LinkedHashMap<>(int capacity, float fillRatio);
     * LinkedHashMap<key-type, value-type> lhm = new LinkedHashMap<>(int capacity, float fillRatio, boolean order);
     * order : true -- last access order
     *         false -- insertion order
     *
     * Supported Operations:
     * + all HashMap supported operations
     * removeEldestEntry: removes the oldest element after the each operation:
     * https://www.geeksforgeeks.org/linkedhashmap-removeeldestentry-method-in-java/
     */




}
